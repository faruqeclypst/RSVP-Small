export interface RSVP {
    id: string;
    name: string;
    affiliation: string;
    guests: number;
  }
  
  export interface LandingPageSettings {
    title: string;
    backgroundImage: string;
  }